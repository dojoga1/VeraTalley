# Dependencies
node_modules/
.pnpm-store/

# Secrets. Never commit any of these. See .env.example for the shape.
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
**/deployments/*.private.json

# Build output
dist/
build/
out/
.next/
.turbo/
*.tsbuildinfo

# Regenerated by Next.js on every `next dev` and `next build`. Committing it
# just produces a conflict every time two people run the dev server.
next-env.d.ts

# Hardhat
artifacts/
cache/
types/generated/
ignition/deployments/

# A local node hands the same deterministic addresses to everyone, so committing
# them tells nobody anything and conflicts every time two people deploy locally.
# deployments/amoy.json IS committed: it is the one record of what is live.
packages/contracts/deployments/localhost.json

# Prisma
packages/db/prisma/*.db
packages/db/prisma/*.db-journal

# Testing
coverage/
.nyc_output/
test-results/
playwright-report/
blob-report/

# Editors and OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
