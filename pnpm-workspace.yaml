packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 does not run dependency install scripts unless they are listed here.
# Add a package only if you know why it needs to run a build step.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - "prisma"
  - "esbuild"
  - "sharp"
